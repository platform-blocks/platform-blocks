export { Alert } from './Alert';
export type {
  AlertProps,
  AlertVariant,
  AlertSeverity,
  // Deprecated aliases — prefer the Alert* names.
} from './types';
export { Alert as default } from './Alert';
